% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_fcbFlowSet.R
\name{cluster_fcbFlowSet}
\alias{cluster_fcbFlowSet}
\title{Defines populations on barcoded datasets}
\usage{
cluster_fcbFlowSet(
  fcbFlowSet,
  channel,
  levels,
  opt = "mixture",
  dist = NULL,
  subsample = 10000,
  trim = 0,
  ret.model = TRUE,
  updateProgress = NULL
)
}
\arguments{
\item{fcbFlowSet}{An fcbFlowSet object post deskewing (at least one channel in the barcodes slot).}

\item{channel}{The name (string) of the channel to be clustered.}

\item{levels}{Integer, the number of barcoding intensities present in the channel.}

\item{opt}{String: \code{"mixture"} (default) for Gaussian mixture modeling,
or \code{"fisher"} for Fisher-Jenks natural breaks.}

\item{dist}{String, one of \code{c("Normal", "Skew.normal", "Tdist")}, passed
to \code{mixsmsn::smsn.mix}. Ignored for \code{opt = "fisher"}.}

\item{subsample}{Integer, number of cells to subsample for model fitting
(default 10000).}

\item{trim}{Numeric between 0 and 1; trims the upper and lower extremes
to exclude outliers (default 0).}

\item{ret.model}{Logical, whether to retain the fitted model (default TRUE).}

\item{updateProgress}{Callback function used in a Shiny context to report
progress (default NULL).}
}
\value{
An fcbFlowSet with clustering slots populated for all frames.
}
\description{
This function allows you to calculate the probability of a cell originating from a given population using
either gaussian mixture modeling or jenks natural breaks classification
}
\examples{
data(jurkatFCB)
data(jurkatFCB_std)
library(flowCore)
fcbfs <- fcbFlowSet(flowSet(list(A = jurkatFCB)))
fcbfs <- deskew_fcbFlowSet(fcbfs, uptake = jurkatFCB_std,
                           channel = "Pacific Blue-A",
                           predictors = c("FSC-A", "SSC-A", "APC-H7-A"))
fcbfs <- cluster_fcbFlowSet(fcbfs, channel = "Pacific Blue-A",
                           levels = 8, opt = "fisher")
}
\seealso{
\code{\link{cluster_fcbFlowFrame}} for single-frame processing,
  \code{\link{deskew_fcbFlowSet}} for the preceding step,
  \code{\link{assign_fcbFlowSet}} for the next step
}
